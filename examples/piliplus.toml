# altgen config for the PiliPlus AltStore source.
# Run: altgen -c examples/piliplus.toml
# Reproduces the original update_source.py output.

[github]
repo = "bggRGjQaUbCoE/PiliPlus"
# token: optional. Precedence: --token > GITHUB_TOKEN env > this value.

[source]
name = "PiliPlus"
subtitle = "Unofficial PiliPlus AltStore source"
description = "Auto-updated AltStore source for PiliPlus"
icon_url = "https://github.com/bggRGjQaUbCoE/PiliPlus/raw/main/assets/images/logo/logo.png"
website = "https://github.com/bebound/PiliPlus-AltStore-Repo"
tint_color = "#73b480"

[app]
name = "PiliPlus"
bundle_identifier = "com.example.PiliPlus"
developer_name = "bggRGjQaUbCoE"
subtitle = "Latest PiliPlus release"
description = "PiliPlus iOS app builds from GitHub releases"
icon_url = "https://github.com/bggRGjQaUbCoE/PiliPlus/raw/main/assets/images/logo/logo.png"
screenshots = [
  "https://raw.githubusercontent.com/bggRGjQaUbCoE/PiliPlus/refs/heads/main/assets/screenshots/home.png",
  "https://raw.githubusercontent.com/bggRGjQaUbCoE/PiliPlus/refs/heads/main/assets/screenshots/bangumi.png",
  "https://raw.githubusercontent.com/bggRGjQaUbCoE/PiliPlus/refs/heads/main/assets/screenshots/dynamic.png",
]
tint_color = "#73b480"
min_os_version = "14.0"

[versions]
strip_v_prefix = true
include_prereleases = false
asset_pattern = "\\.ipa$"
build_version_pattern = "\\+(\\d+)\\.ipa$"
# The version defaults to the release tag (strip_v_prefix removes a leading "v").
# If a release tag carries no version (e.g. youproextra-ipa2), configure a
# regex to extract it from the release name or IPA filename instead; on no
# match the version falls back to the tag-derived value:
# version_pattern = "_([0-9]+[.][0-9]+[.][0-9]+_[0-9]+[.][0-9]+[.][0-9]+)$"  # e.g. YouProExtra_21.24.3_1.3.1 -> 21.24.3_1.3.1
# version_source = "release"    # "release" (default, release name field) or "filename" (IPA filename)
# max_versions unset: only the newest version is emitted (0 = all versions)

[news]
enabled = true
# placeholders: {name} {version} {tag} {date}
# title_template defaults to "{name} {version} - {date}" — e.g. "PiliPlus 2.1.0 - 12 Jul 2026"
# caption_template = "New {name} update available!"   # default: "{name} {version} is available."
# image_url = "https://example.com/news.png"          # omitted when unset
# news appID comes from [app] bundle_identifier
max_entries = 0

[output]
path = "apps.json"
